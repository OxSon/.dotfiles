- defaults:
    link:
        create: true
        relink: true
        force: false

# - clean: ['~', '~/.config']

- link:
    ~/.ideavimrc:
    ~/.config/kitty:
        glob: true
        path: config/kitty/*
    ~/.ghci:
    ~/.emacs:

    # fix
    # ~/.mozilla/firefox/*.default/chrome/:
    #     glob: true
    #     path: chrome/*

    # fix colors
    # ~/.Xresources:

    # merge with existing
    # ~/.zshrc:
    # ~/.vim:
    # ~/.vimrc:
    # ~/.bashrc:


- shell:
    - git submodule update

