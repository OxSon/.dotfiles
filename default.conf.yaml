- defaults:
    link:
        create: true
        relink: true

- clean: ['~' '~/.config']

- link:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.bashrc:
    ~/.zshrc:
    ~/.ghci/:
    ~/.ideavimrc:
    ~/.taskrc:
    ~/.config/:
        glob: true
        path: config/*

