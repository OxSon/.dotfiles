- defaults:
    link:
        create: true
        relink: true
        force: true

- clean: ['~', '~/.config']

- link:
    ~/.tmux.conf:
    ~/.Xresources:
    ~/.ghci:
    ~/.vim:
    ~/.vimrc:
    ~/.bashrc:
    ~/.zshrc:
    ~/.ideavimrc:
    ~/.taskrc:
    ~/.config/:
        glob: true
        path: config/*

